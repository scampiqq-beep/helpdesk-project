# Automation Engine — Step 7 (Services export fix + preview UI foundation)

Этот шаг:
- исправляет ImportError по `NotificationService` и `ProfileService`
- восстанавливает compatibility-экспорты в `helpdesk_app.services`
- добавляет foundation-пометки для следующего UI-preview шага автоматизации

После этого существующие route-модули должны снова импортироваться нормально.
