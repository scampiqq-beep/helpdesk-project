SLA v2 — шаг 5

Что сделано:
- добавлены синтетические SLA-события для audit/timeline без новой таблицы БД;
- улучшен расчёт дедлайнов по рабочему календарю;
- `recalculate_open_tickets` теперь пересчитывает дедлайн через `SLAService`;
- исправлен runtime-баг в `KnowledgeService` (`KnowledgeBaseCategory.query` и связанные импорты);
- исправлены недостающие импорты в `HistoryService` и `AuditService`.

Что дальше:
- вывести SLA events в карточку заявки отдельным блоком;
- добавить SLA compliance в отчёты;
- затем перейти к внутренним комментариям и шаблонам ответов.
